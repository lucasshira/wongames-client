import type { Meta, StoryObj } from '@storybook/react'
import Menu from '.'

export default {
  title: 'Menu',
  component: Menu,
  tags: ['autodocs'],
} as Meta<typeof Menu>

export const Default: StoryObj<typeof Menu> = {}